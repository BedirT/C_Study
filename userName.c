#include<stdio.h>#include<stdlib.h>int main () {        // User Name    // 6 digits    // Only Numbers    // No spaces    // Constant-123456        int constUserName = 123456;        // Password    // 4 digits    // Only Numbers    // No spaces    // temp-1234    int password = 1234;        int enteredUN, enteredP, attempt = 3;        while (attempt > 0) {                printf("Username: ");        scanf("%d",&enteredUN);                printf("Password: ");        scanf("%d",&enteredP);             if ( constUserName != enteredUN || enteredP != password ) {            attempt--;            printf("Invalid credentials program terminates after 3 failed attemts.\nNumber of failed attempts = %d\n", 3-attempt);        }        else {            break;        }        if ( attempt == 0) {            printf("Goodbye!\n");            exit;        }    }        do {                printf("Choose your new password\n(4 digits---Different from temp one)\n");        scanf("%d", &password);            } while ( password<999 || password>10000 || password == 1234 );        printf("You have changed your password succesfully. Please re-login.\n");    attempt = 3;    while (attempt > 0) {                printf("Username: ");        scanf("%d",&enteredUN);                printf("Password: ");        scanf("%d",&enteredP);                if ( constUserName != enteredUN || enteredP != password ) {            attempt--;            printf("Invalid credentials program terminates after 3 failed attemts.\nNumber of failed attempts = %d\n", 3-attempt);        }        else {            printf("Access is granted :O \n");            break;        }        if ( attempt == 0) {            printf("Goodbye!\n");            exit;        }    }            return 0;}